language: node_js
node_js:
  - "0.10"
before_script:
  - npm install -g grunt-cli
  - npm install
  - npm install -g bower
  - bower install
branches:
  except:
    - /^v1\..*/
    - /^1\..*/
    - gh-pages
